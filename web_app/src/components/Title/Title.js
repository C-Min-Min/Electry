import React from 'react'
import { Helmet } from 'react-helmet';

const Title = () => {
    return (
        <Helmet>
            <title>Electry</title>
        </Helmet>
    );
}

export default Title;